query EntityByPath($path: String!) {
  route(path: $path) {
    ... on EntityCanonicalUrl {
      entity {
        __typename
        entityMetatags {
          __typename
          key
          value
        }
        # Fields fragments depending on node type
        ...NodePageFields
      }
    }
  }
}
