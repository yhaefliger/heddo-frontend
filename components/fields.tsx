//TODO: like for paragraphs map fields and display specific or default render for field

const FieldsComponents = {}

type Props = {
  fields: {
    [key: string]: string | object
  }
}

const Fields = ({ fields }: Props) => {
  return <></>
}

export default Fields
